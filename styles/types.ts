import { UnistylesThemes } from 'react-native-unistyles';

type Color = UnistylesThemes['light']['color'];

type Utils = UnistylesThemes['light']['utils'];

type Width = UnistylesThemes['light']['width'];

type Space = UnistylesThemes['light']['space'];

type Height = UnistylesThemes['light']['height'];

export { type Color, type Utils, type Width, type Space, type Height };
